import { gotServer } from './'

describe('.gotServer()', () => {
  describe('returns .get() and', () => {
    it('can make GET requests', async () => {
      const request = gotServer(123);

      console.log(1)
    })
  })
})
